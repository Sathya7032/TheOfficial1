{
    "builds":[{
        "src": "project/wsgi.py",
        "use":"@vercel/python",
        "config":{"maxLambdasize":"15mb","runtime":"python3.11.3"}
    }],
    "routes":[
        {
            "src": "/(.*)",
            "dest": "project/wsgi.py"
        }
    ]
}
